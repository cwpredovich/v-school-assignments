import React from 'react';
import ReactDOM from 'react-dom';
import Form from './Form';

ReactDOM.render(<Form />, document.getElementById('root'));


// this page is not functional because I never ran "create-react-app ." in terminal within the directory BEFORE typing all of this out
// the functional version is in the directory "react-forms-2"